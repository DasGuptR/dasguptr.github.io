{
  "hash": "ae73e980a44bfd9f77b4b606f3399ca9",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Comparison: Das Gupta & Horiuchi\"\nformat: html\neditor_options: \n  chunk_output_type: console\nexecute:\n  warning: false\n  message: false\n---\n\n\n\n\n\n\nThis documents provides some comparisons of the Horiuchi and Das Gupta methods of decomposition, as implemented in **{{DemoDecomp}}** and **{{DasGuptR}}** respectively. \n\nFirst, it shows for a simple decomposition of a rate as the product of four factors, that the two approaches converge as the number of integration steps for the Horiuchi method increases. \nIt then demonstrates that the number of factors and the numbers of subgroups create little divergence between the two methods, nor does the size of the crude rate difference being decomposed. \n\nFinally, it provides a discussion of subgroup specific contributions as calculated via the two methods.  \n\n# 1. Setup and functions\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nlibrary(tidyverse)\nlibrary(DemoDecomp)\nlibrary(DasGuptR)\n\n\n## Data sim --- \n# Two populations, nK factors, nI subgroups,\n# difference in crude rates should be ~0.05\nsimdata <- function(nK = 3, nI = 5, crude_delta = 0.05) {\n  # subgroup size:\n  wA = rgamma(nI, shape = 2, scale = 1000)\n  # subgroup prop:\n  pA = wA / sum(wA)\n  \n  # have f1 be a baseline rate: \n  fA1 = rbeta(nI, shape1 = 3, shape2 = 50) \n  # make f2..k multipliers of relative risk, ~1\n  if(nK > 1) {\n    fA2k = rlnorm(nI * (nK - 1), meanlog = 0, sdlog = 0.25)\n    fA = c(fA1, fA2k)\n  } else {\n    fA = fA1\n  }\n  \n  \n  # pop B is initially pop A:\n  pB = pA\n  fB = fA\n  \n  # add some shift:\n  pB = pA + rnorm(nI, mean = 0, sd = 0.02)\n  # needs to be bounded\n  pB = pmax(pB, 0.001) / sum(pmax(pB, 0.001))\n  # F1 will increase by crude_delta on avg. \n  # F2..k are es\n  fB[1:nI] <- \n    pmax(\n      fB[1:nI] + crude_delta + rnorm(nI, mean = 0, sd = crude_delta*.1), \n      .0001\n      )\n  if (nK > 1) {\n    fB[(nI+1):length(fB)] <- fB[(nI+1):length(fB)] * \n      rlnorm(nI * (nK - 1), meanlog = 0, sdlog = 0.05)\n  }\n  \n  return(list(\n    popA = c(pA, fA),\n    popB = c(pB, fB),\n    nK = nK,\n    nI = nI\n  ))\n}\n\n\n\n## Das Gupta decomposition --- \ngetDG <- function(sdata){\n  require(DasGuptR)\n  # dgnpop needs a data frame,\n  # and population indicator etc. \n  dfdg <- cbind(\n    pop = rep(letters[1:2],e=sdata[[4]]),\n    sgroup = rep(1:sdata[[4]],2),\n    rbind(\n      as.data.frame(matrix(sdata[[1]],ncol=sdata[[3]]+1)),\n      as.data.frame(matrix(sdata[[2]],ncol=sdata[[3]]+1))\n    )\n  )\n\n  ## decomp:\n  if(sdata[[4]]==1){\n    # subgroup size not included if only 1 subgroup\n    flist = names(dfdg)[-c(1:3)] \n  } else{\n    flist = names(dfdg)[-c(1:2)] \n  }\n  \n  dg_res = \n    dg_table(dgnpop(dfdg,\n         pop = \"pop\", \n         factors = flist,\n         id_vars = \"sgroup\",\n         quietly = FALSE,\n         ratefunction = \n           paste0(\"sum(\",paste0(flist,collapse=\"*\"),\")\")\n  ))$diff\n  \n  return(\n    # remove the crude diffs:\n    dg_res[-length(dg_res)]\n  )\n}\n\n## Horiuchi decomposition --- \ngetH <- function(sdata, nstep=100){\n  require(DemoDecomp)\n  # horiuchi just wants vectors\n  pop1 = sdata[[1]]\n  pop2 = sdata[[2]]\n  \n  # rate function\n  if(sdata[[4]]==1){\n    \n    aggrfunc = function(x){\n      x <- matrix(x, ncol = sdata[[3]])\n      sum(matrixStats::rowProds(x))\n    }\n    # decomposition:\n    hres <- horiuchi(func = aggrfunc,\n                   pars1 = pop1[-1],\n                   pars2 = pop2[-1],\n                   N = nstep)\n    hres <- matrix(hres, ncol = sdata[[3]])\n    \n  } else {\n    \n    aggrfunc = function(x){\n      x <- matrix(x, ncol = sdata[[3]]+1)\n      sum(matrixStats::rowProds(x))\n    }\n    # decomposition:\n    hres <- horiuchi(func = aggrfunc,\n                   pars1 = pop1,\n                   pars2 = pop2,\n                   N = nstep)\n    hres <- matrix(hres, ncol = sdata[[3]]+1)\n    \n  }\n\n  return(\n    colSums(hres)\n  )\n}\n```\n:::\n\n\n\n\n## 2. Number of integration steps\n\nFor this example, we set the number of factors = 4, and the number of subgroups = 1. \nThe rate is a simple product of the 4 factors, where the crude rate difference is set to approximately .05 (rates are expressed as proportions).  \nWe vary the number of integration steps in Horiuchi (the `N` argument to `horiuchi()` function), from 1 to 100. \nFor each number of integration steps, we sim 50 datasets and perform decomposition using both `DasGuptR::dgnpop()` and `DemoDecomp::horiuchi()`.  \n\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# set up conditions and n sims\nres1 <- \n  expand_grid(\n    nsteps = c(1:25,seq(30,100,5)),\n    sims = 1:50\n  )\n\n# for each sim, get decomposition effects from \n# both DG and H methods\nres1$data <- res1$DG <- res1$H <- NA\nfor(i in 1:nrow(res1)){\n  res1$data[i] <- list(simdata(nK=4,nI=1))\n  res1$DG[i] <- list(getDG(res1$data[[i]]))\n  res1$H[i] <- list(getH(res1$data[[i]], res1$nsteps[[i]]))\n}\n\nres1 <- res1 |> unnest(c(DG,H))\n\nsave(res1, file = \"dgh_res1.rdata\")\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nload(url(\"https://dasguptr.github.io/dgh_res1.rdata\"))\n\ncbind(\n  nsteps = c(1:25,seq(30,100,5)),\n  rmse = tapply((res1$H - res1$DG)^2, \n                res1$nsteps, \n                \\(x) sqrt(mean(x))\n                )\n  ) |> \n  ggplot(aes(x=nsteps, y=rmse)) +\n  geom_point()+geom_line()+\n  labs(y = \"root mean squared diff\\nHoriuchi - Das Gupta\")\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-4-1.png){width=672}\n:::\n:::\n\n\n\n## 3. Number of factors\n\nFor Das Gupta's approach, increasing the number of factors involves increased computation time, as it means evaluating the rate at many more combinations of factors.  \n\nBelow, we vary the number of factors from 1 to 10, again with just a single subgroup, and a crude rate difference of ~.05.  \nIn each case, the rate is calculated as a simple product of all factors, and we use 100 integration steps for Horiuchi.  \nFor each number of factors, we simulate just 15 datasets.  \n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# set up conditions and n sims\nres2 <- \n  expand_grid(\n    nf = c(2:10),\n    sims = 1:15\n  )\n\n# for each sim, get decomposition effects from \n# both DG and H methods\nres2$data <- res2$DG <- res2$H <- NA\nfor(i in 1:nrow(res2)){\n  res2$data[i] <- list(simdata(nK=res2$nf[i], nI=1))\n  res2$DG[i] <- list(getDG(res2$data[[i]]))\n  res2$H[i] <- list(getH(res2$data[[i]], nstep = 100))\n}\n\nres2 <- res2 |> unnest(c(DG,H))\nsave(res2, file = \"dgh_res2.rdata\")\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nload(url(\"https://dasguptr.github.io/dgh_res2.rdata\"))\n\ncbind(\n  nf = c(2:10),\n  rmse = tapply((res2$H - res2$DG)^2, \n                res2$nf, \n                \\(x) sqrt(mean(x))\n                )\n  ) |> \n  ggplot(aes(x=nf, y=rmse)) +\n  geom_point()+geom_line()+\n  labs(y = \"root mean squared diff\\nHoriuchi - Das Gupta\",\n       x = \"number of factors\")\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-6-1.png){width=672}\n:::\n:::\n\n\n\n\n## 4. Number of subgroups: \n\nFinally, we vary the number of subgroups from 2 to 50. \nIn each case, the rate is calculated as the product of 2 factors (subgroups relative sizes, and a subgroup specific value such as a subgroup specific rate), and we use 100 integration steps for Horiuchi.  \nFor each number of subgroups, we simulate 50 datasets.  \n\nHere we are only comparing the divergence in the marginal effects of each factor (i.e., the sum of the subgroup specific contributions).  \n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nres3 <- \n  expand_grid(\n    nsubgroup = c(2:50),\n    sims = 1:50\n  )\n\nres3$data <- res3$DG <- res3$H <- NA\nfor(i in 1:nrow(res3)){\n  res3$data[i] <- list(simdata(nK=1,nI=res3$nsubgroup[i]))\n  res3$DG[i] <- list(getDG(res3$data[[i]]))\n  res3$H[i] <- list(getH(res3$data[[i]], nstep = 100))\n}\n\nres3 <- res3 |> unnest(c(DG,H))\nsave(res3, file=\"dgh_res3.rdata\")\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nload(url(\"https://dasguptr.github.io/dgh_res3.rdata\"))\n\ncbind(\n  nsubgroup = c(2:50),\n  rmse = tapply((res3$H - res3$DG)^2, \n                res3$nsubgroup, \n                \\(x) sqrt(mean(x))\n                )\n  ) |> \n  ggplot(aes(x=nsubgroup, y=rmse)) +\n  geom_point()+geom_line()+\n  labs(y = \"root mean squared diff\\nHoriuchi - Das Gupta\")\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-8-1.png){width=672}\n:::\n:::\n\n\n\n\n## 5. Crude rate difference \n\nNote that these differences will grow ever so slightly the bigger the crude-rate differences are, but these are always minimal (<1e-6)\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n# set up conditions and n sims\nres4 <- \n  expand_grid(\n    cdiff = seq(0,1,length.out=20),\n    sims = 1:50\n  )\n\n# for each sim, get decomposition effects from \n# both DG and H methods\nres4$data <- res4$DG <- res4$H <- NA\nfor(i in 1:nrow(res4)){\n  res4$data[i] <- list(simdata(nK=4,nI=1,crude_delta=res4$cdiff[i]))\n  res4$DG[i] <- list(getDG(res4$data[[i]]))\n  res4$H[i] <- list(getH(res4$data[[i]], 100))\n}\n\nres4 <- res4 |> unnest(c(DG,H))\nsave(res4, file=\"dgh_res4.rdata\")\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\nload(url(\"https://dasguptr.github.io/dgh_res4.rdata\"))\n\ncbind(\n  cdiff = seq(0,1,length.out=20),\n  rmse = tapply((res4$H - res4$DG)^2, \n                res4$cdiff, \n                \\(x) sqrt(mean(x))\n                )\n  ) |> \n  ggplot(aes(x=cdiff, y=rmse)) +\n  geom_point()+geom_line()+\n  labs(y = \"root mean squared diff\\nHoriuchi - Das Gupta\",\n       x = \"crude rate difference\")\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-10-1.png){width=672}\n:::\n:::\n\n\n\n\n## 6. subgroup specific effects and sum-to-1 constraints\n\nHere we demonstrate how it is possible to reparameterize Horiuchi by dropping any given subgroup proportion to obtain different subgroup specific contributions (all relative to the omitted group), but preserve the overall marginal totals. \nWe also show that this is not possible in Das Gupta's method - it is possible to omit a subgroup proportion but this makes no change to the decomposition process.  \nFor this demonstration, we will use Das Gupta's Example 5.1, which contains, for 2 populations, a set of 13 age groups, the age-specific rates, and their relative size as a percentage.  \nA quick plot of the age group sizes shows that, e.g.: the 15-19 group makes up a bigger proportion of the 1st population than the 2nd; this is reversed in the 25-29 group; and the 60-64 group makes up a similar proportion in both populations:  \n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# express subgroup size as proportions (currently %)\ndf <- dgeg5_1 |> mutate(size=size/100)\n\nggplot(df,aes(x=pop,y=size))+\n  geom_point()+geom_line(group=1)+\n  facet_wrap(~age_group)\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-11-1.png){width=672}\n:::\n:::\n\n\n\nPerforming decomposition with `DemoDecomp::horiuchi()` requires providing the values as vectors of length K*I (where K is number of factors, I is number of subgroups): \n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# population weights and rates as vectors for horiuchi()\npop1 = c(df$size[df$pop==1970],df$rate[df$pop==1970])\npop2 = c(df$size[df$pop==1985],df$rate[df$pop==1985])\n\n# this is the rate function for decomposition using full vector: \naggrfunc_full <- function(x){\n  ws = x[1:13]\n  rs = x[14:26]\n  sum(ws*rs)\n}\n\nhres0 <- horiuchi(func = aggrfunc_full,\n                  pars1 = pop1,\n                  pars2 = pop2,\n                  N = 100)\n```\n:::\n\n\n\nThe overall factor contributions (age-proportions and age-specific rates) are obtained by summing over the subgroups:  \n\n\n::: {.cell}\n\n```{.r .cell-code}\nsum(hres0[1:13]);sum(hres0[14:26])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.2269\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.7403\n```\n\n\n:::\n:::\n\n\n\nWe can see that these are equivalent to what we get out of `DasGuptR::dgnpop()` (the `diff` column of the table below): \n\n\n::: {.cell}\n\n```{.r .cell-code}\ndgnpop(df, pop=\"pop\", \n       factors=c(\"size\",\"rate\"),\n       ratefunction = \"sum(size*rate)\") |>\n  dg_table()\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n         1970    1985   diff decomp\nsize  45.5876 46.8145 1.2269  41.35\nrate  45.3309 47.0712 1.7403  58.65\ncrude 44.7268 47.6940 2.9672 100.00\n```\n\n\n:::\n:::\n\n\n\nTo get subgroup specific effects from `dgnpop()`, we need to set `agg = FALSE` as well as removing the aggregating `sum()` from the rate function.  \n\n\n::: {.cell}\n\n```{.r .cell-code}\ndgres0 <- \n  dgnpop(df, pop=\"pop\", \n         factors=c(\"size\",\"rate\"),\n         id_vars = \"age_group\",\n         ratefunction = \"size*rate\", \n         agg=FALSE) \n\n# to get these into the same format as horiuchi: \ndgres0 <- \n  dgres0[,-3] |>\n  pivot_wider(names_from=factor,values_from=rate) |>\n  pivot_wider(names_from=pop,values_from=c(size,rate)) |>\n  transmute(\n    size = size_1985-size_1970,\n    rate = rate_1985-rate_1970\n  )\n```\n:::\n\n\n\nJust as with Horiuchi, the overall factor contributions can be retrieved by summing over the subgroups: \n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolSums(dgres0)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  size   rate \n1.2269 1.7403 \n```\n\n\n:::\n:::\n\n\n\nAnd the subgroup specific values are the same for the two methods:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncbind(\n  dgres0,\n  hres0[1:13],\n  hres0[14:26]\n) |> plot()\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-17-1.png){width=672}\n:::\n:::\n\n\n\nBecause subgroup proportions must sum to 1, we can omit a group from the vector, and provide this as part of the function.  \n\nFor `horiuchi()`, we need to adjust the function to insert the missing value in the appropriate place in the vector:  \n\n\n::: {.cell}\n\n```{.r .cell-code}\naggrfunc_sub <- function(x, idx){\n  # put back in the missing group proportion, as 1-sum(others):\n  # note that x is now of length (I*K)-1. \n  ws = append(x[1:12], 1-sum(x[1:12]), after = idx-1)\n  rs = x[13:25]\n  sum(ws*rs)\n}\n```\n:::\n\n\n\nWe can then remove a given subgroup proportion: \n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 3rd subgroup weight is missing: \nhres_sub3 <- horiuchi(func = aggrfunc_sub,\n                 pars1 = pop1[c(1:2,4:26)],\n                 pars2 = pop2[c(1:2,4:26)],\n                 N = 100,\n                 idx = 3)\n\n# 4th subgroup weight is missing: \nhres_sub4 <- horiuchi(func = aggrfunc_sub,\n                 pars1 = pop1[c(1:3,5:26)],\n                 pars2 = pop2[c(1:3,5:26)],\n                 N = 100,\n                 idx = 4)\n```\n:::\n\n\n\nThe overall marginal totals remain the same: \n\n\n::: {.cell}\n\n```{.r .cell-code}\n# full vector\nsum(hres0[1:13]);sum(hres0[14:26])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.2269\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.7403\n```\n\n\n:::\n\n```{.r .cell-code}\n# missing group 1 prop\nsum(hres_sub3[1:12]);sum(hres_sub3[13:25])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.2269\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.7403\n```\n\n\n:::\n\n```{.r .cell-code}\n# missing group 6 prop:\nsum(hres_sub4[1:12]);sum(hres_sub4[13:25])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.2269\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1.7403\n```\n\n\n:::\n:::\n\n\n\nBut the individual contributions for the subgroups are different: \n\n\n::: {.cell}\n\n```{.r .cell-code}\ncbind(\n  full_w = hres0[1:13],\n  sub3_w = append(hres_sub3[1:12], NA, after=2),\n  sub4_w = append(hres_sub4[1:12], NA, after=3)\n) |> \n  as.data.frame() |> plot()\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-21-1.png){width=672}\n:::\n:::\n\n\n\n\nWe can attempt the same using Das Gupta's approach, removing a sub-group proportion and including the sum to 1 constraint within the calculation of the rate:  \n\n\n::: {.cell}\n\n```{.r .cell-code}\ndgfunc <- function(size,rate,idx){\n  size[idx] <- 1-sum(size)\n  size*rate\n}\n```\n:::\n\n\n\nBecause `dgnpop()` takes a dataframe object, and it doesn't like missing data, we can set the omitted group proportion as zero in both populations. \n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 3rd subgroup weight is missing: \ndfA <- df\ndfA$size[df$age_group==\"25-29\"] <- 0\n\ndgres_sub3 <- \n  dgnpop(dfA,\n       pop=\"pop\", factors=c(\"size\",\"rate\"),\n       id_vars = \"age_group\",\n       ratefunction = \"dgfunc(size,rate,3)\",agg=FALSE) |>\n  select(-std.set) |>\n  pivot_wider(names_from=factor,values_from=rate) |>\n  pivot_wider(names_from=pop,values_from=c(size,rate)) |>\n  transmute(\n    size = size_1985-size_1970,\n    rate = rate_1985-rate_1970\n  )\n\n# 2nd subgroup weight is missing: \ndfA <- df\ndfA$size[df$age_group==\"30-34\"] <- 0\n\ndgres_sub4 <- \n  dgnpop(dfA,\n       pop=\"pop\", factors=c(\"size\",\"rate\"),\n       id_vars = \"age_group\",\n       ratefunction = \"dgfunc(size,rate,4)\",agg=FALSE) |>\n  select(-std.set) |>\n  pivot_wider(names_from=factor,values_from=rate) |>\n  pivot_wider(names_from=pop,values_from=c(size,rate)) |>\n  transmute(\n    size = size_1985-size_1970,\n    rate = rate_1985-rate_1970\n  )\n```\n:::\n\n\n\nAgain, the overall marginal totals match:\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# full data\ncolSums(dgres0)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  size   rate \n1.2269 1.7403 \n```\n\n\n:::\n\n```{.r .cell-code}\n# missing group 1 prop\ncolSums(dgres_sub3)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  size   rate \n1.2269 1.7403 \n```\n\n\n:::\n\n```{.r .cell-code}\n# missing group 2 prop:\ncolSums(dgres_sub4)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  size   rate \n1.2269 1.7403 \n```\n\n\n:::\n:::\n\n\n\nAnd in this case, the subgroup specific contributions are invariant to which groups proportion was omitted:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncbind(\n  full_w = dgres0$size,\n  sub3_w = dgres_sub3$size,\n  sub4_w = dgres_sub4$size\n) |> \n  as.data.frame() |> plot()\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-25-1.png){width=672}\n:::\n:::\n\n\n\n\nTo demonstrate where these two methods diverge, consider a scenario in which there are two populations, 20 subgroups, and 3 factors (the first of these are relative sizes of subgroups). \n\nThe population rate is calculated as $R = \\sum_i a_i \\cdot b_i \\cdot c_i$\n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n## Data: \nsdata <- simdata(nK=2,nI=20)\n\n## Das Gupta: \ndfdg <- cbind(\n  pop = rep(c(\"pop1\",\"pop2\"),e=20),\n  sgroup = rep(1:20,2),\n  rbind(\n    as.data.frame(matrix(sdata[[1]],ncol=3)),\n    as.data.frame(matrix(sdata[[2]],ncol=3))\n  )\n)\nnames(dfdg)[3:5] <- letters[1:3]\n\ndg_res <- \n  dgnpop(dfdg, pop = \"pop\", \n         factors = c(\"a\",\"b\",\"c\"),\n         id_vars = \"sgroup\", \n         ratefunction = \"a*b*c\", \n         agg=FALSE) |>\n  pivot_wider(names_from=factor,values_from=rate) |>\n  select(-std.set) |>\n  pivot_wider(names_from=pop,values_from=c(a,b,c)) |>\n  transmute(\n    a_diff = a_pop2-a_pop1,\n    b_diff = b_pop2-b_pop1,\n    c_diff = c_pop2-c_pop1\n  )\n\n\n## Horiuchi:  \npop1 = sdata[[1]]\npop2 = sdata[[2]]\n\naggrfunc = function(x){\n    x <- matrix(x, ncol = 3)\n    sum(x[,1] * x[,2] * x[,3])\n}\n\nhres <- horiuchi(func = aggrfunc,\n                  pars1 = pop1,\n                  pars2 = pop2,\n                  N = 100)\n\nhres <- matrix(hres, ncol = 3)\n```\n:::\n\n\n\nThe marginal sums of subgroup contributions are very close (as seen above). \n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolSums(hres)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.0006400499 0.0546305565 0.0011387307\n```\n\n\n:::\n\n```{.r .cell-code}\ncolSums(dg_res)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n      a_diff       b_diff       c_diff \n0.0006400518 0.0546305583 0.0011387325 \n```\n\n\n:::\n:::\n\n\n\nAnd the individual subgroup effects are also similar:\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\npar(mfrow=c(1,3))\ncbind(DG=dg_res[[1]], H=hres[,1]) |> plot(main=\"V1\")\ncbind(DG=dg_res[[2]], H=hres[,2]) |> plot(main=\"V2\")\ncbind(DG=dg_res[[3]], H=hres[,3]) |> plot(main=\"V3\")\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-28-1.png){width=672}\n:::\n:::\n\n\n\nHowever, when we move to a rate that involves a cross-group dependency, such as $R = \\sum_i a_i \\cdot \\frac{b_i}{\\sum b} \\cdot c_i$. \n\nHere, because $b$ is **not** constrained to sum to 1 (as $a$ is, because $a$ is the subgroup proportions), this means that contribution of $b_i$ is dependent on the values that other groups have for $b$.  \n\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\ndg_res1 <- \n  dgnpop(dfdg, \n         pop = \"pop\", \n         factors = c(\"a\",\"b\",\"c\"),\n         id_vars = \"sgroup\", \n         ratefunction = \"a * (b / sum(b)) * c\", agg=FALSE) |>\n  pivot_wider(names_from=factor,values_from=rate) |>\n  select(-std.set) |>\n  pivot_wider(names_from=pop,values_from=c(a,b,c)) |>\n  transmute(\n    a_diff = a_pop2-a_pop1,\n    b_diff = b_pop2-b_pop1,\n    c_diff = c_pop2-c_pop1\n  )\n\npop1 = sdata[[1]]\npop2 = sdata[[2]]\n\naggrfunc = function(x){\n    x <- matrix(x, ncol = 3)\n    sum(x[,1] * (x[,2] / sum(x[,2])) * x[,3])\n}\nhres1 <- horiuchi(func = aggrfunc,\n                  pars1 = pop1,\n                  pars2 = pop2,\n                  N = 100)\nhres1 <- matrix(hres1, ncol = 3)\n```\n:::\n\n\n\nNow we start to see differences in both marginal totals (slightly more so for $b$), and subgroup specific $b$ contributions: \n\n\n::: {.cell}\n\n```{.r .cell-code}\ncolSums(hres1)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.0004630014 0.0016597231 0.0008011704\n```\n\n\n:::\n\n```{.r .cell-code}\ncolSums(dg_res1)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n      a_diff       b_diff       c_diff \n0.0005061009 0.0016081531 0.0008096777 \n```\n\n\n:::\n:::\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\npar(mfrow=c(1,3))\ncbind(DG=dg_res1[[1]], H=hres1[,1]) |> plot(main=\"V1\")\ncbind(DG=dg_res1[[2]], H=hres1[,2]) |> plot(main=\"V2\")\ncbind(DG=dg_res1[[3]], H=hres1[,3]) |> plot(main=\"V3\")\n```\n\n::: {.cell-output-display}\n![](dasguptr_horiuchi_files/figure-html/unnamed-chunk-31-1.png){width=672}\n:::\n:::\n",
    "supporting": [
      "dasguptr_horiuchi_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}